Ámbito
======

El *software* se ejecutará como un proceso *daemon*, cuyo comportamiento se definirá a través de ficheros de configuración e interacciones con el gestor de servicios del sistema operativo (el *software* es compatible con **systemd** e **initd**). Los diferentes registros se almacenarán en un fichero de *log*.